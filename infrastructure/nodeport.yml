apiVersion: v1
kind: Service
metadata:
  name: todoapp-node
  namespace: todoapp
spec:
  type: NodePort
  selector:
    app: todoapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30001
